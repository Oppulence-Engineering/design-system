/**
 * `@oppulence/infinite-canvas/panels` — shipped UI chrome (layers, inspector, toolbar).
 * Depends on the optional design-system peer + react-colorful. Consumers wanting fully
 * custom chrome should import from `./headless` instead.
 */

export { CanvasLayersPanel } from "./layers-panel";
export { CanvasInspectorPanel } from "./inspector-panel";
export { CanvasToolbar } from "./toolbar";
export { CanvasPalette } from "./palette";
