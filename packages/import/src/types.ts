/**
 * @module @oppulence/import/types
 * @file Type definitions for the import package.
 * @author Canvas Team
 * @deprecated Use @oppulence/import/schemas for all type definitions
 * @copyright 2024 Canvas
 */

// Re-export types from the schemas module for backward compatibility
export type { RawTransaction, Transaction } from "./schemas";
